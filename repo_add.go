package engine

func (e *Engine) AddRepository(url string) (*Repository, error) {
	/* clone to tmp dir
	 * read .go-template.yml
	 * [resolve conflicting names]
	 * move into place
	 */
	return nil, nil
}
